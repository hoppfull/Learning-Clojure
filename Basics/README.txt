These examples require clojure-1.6.0.jar in this directory to work.

Instead of copying clojure-1.6.0.jar into every single example, I make sure
the run.bat files all call the same file in this directory!